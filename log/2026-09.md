# 工作记录 2026-09

只追加。再小的改动也记一条。每条以 `## 日期 做了什么 [执行者]` 开头，下面一到三行要点：做了什么或决定了什么、结果、任务文件夹或文件。

## 2026-09-23 工作区初始化 [CC]

- 按工作区规范 v1.2 新建：AGENTS.md、CLAUDE.md、START_HERE.md、work/、log/、log/2026-09.md。
- 目录原为空。

## 2026-09-23 建立通用版配置 [CC]

- 整理出不限专业的全局规则、回复风格、交接、两个对话分工、子代理委派、授权书模板、工作区规范 v1.2 与脚本、Codex 和 Claude Code 子代理，以及技能清单；由使用者的 agent 按 `INSTALL.md` 安装，不改安全、权限和账号设置。
- 在临时目录里按 `INSTALL.md` 完整演练一遍，发现的三处说明问题已改。任务文件夹：`work/2026-09/2026-09-23-initial-build/`（维护者本地，不发布）。

## 2026-09-23 加 MIT 许可 [CC]

- 仓库改为公开后，按用户要求加 MIT 许可（`LICENSE`，版权人写 GitHub 账号名）；draw.io 技能文件夹里放一份副本，单独安装时声明随它走。README 和技能清单的许可说明同步。

## 2026-09-23 子代理改为三档，结果存进项目文件 [CC]

- 去掉 clerk 和 terra 两档，合并为 `luna_worker`（Codex 用 gpt-6-luna、推理强度按任务定；Claude Code 用 Sonnet）；`critical_reviewer` 改为最强模型 high，`professional_reviewer` 为 max，Claude Code 两个评审都用 Fable。
- 子代理和分支对话的完整结果存进任务文件夹的 `agents/`、`messages/`，回到主对话的只是摘要和路径；工作区规范 v1.3 加了 `workspace.py keep`，从 Claude Code 或 Codex 的记录里取回子代理的任务说明和最后回复。

## 2026-09-23 Claude Code 评审改用 Opus [CC]

- 两个 Claude Code 评审改用 Opus（high、max），Fable 只在用户指定时使用。

## 2026-09-24 子代理只在划算时派 [CC]

- 子代理冷启动要先读约 2.5 万 token 的说明，回复还要再读一遍，所以小事不派；规则和委派指南改为只在原始材料远多于带回结论时委派。

## 2026-09-24 Claude 子代理不读规则文件 [CC]

- 实测 Claude Code 子代理冷启动约 2.4 万 token，其中约 7–8 千是规则文件；三个子代理加 `omitClaudeMd: true`，项目约束写在派发说明里。

## 2026-09-24 限制对话间通信和自动任务 [CC]

- 对话之间只在派发、终点结果、需要对方决定时通信；自动任务只做状态查询，有结果才处理。

## 2026-09-24 规则精简为宏观原则 [CC]

- 全局规则只留原则和底线（约 8.4k → 3.6k 字符）；冷启动数字、角色与推理强度、简报格式、留档命令、自动任务配方移到 `DELEGATION.md`，“派发时写明推理强度”写进 Codex 评审角色的说明。
- 分工、交接、委派指南各压短约三成到一半，回复风格正文压短、例子不变；Claude Code 仍导入回复风格，不再导入工作区规范（初始化或检查工作区时再读）。
